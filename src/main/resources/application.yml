spring:
  application:
    name: Spring-In-Action
  datasource:
    generate-unique-name: false
    name: tacocloud
    username: sa
    password:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.H2Dialect
server:
  port: 8081